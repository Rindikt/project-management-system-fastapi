# 🚀 Система управления проектами на FastAPI и PostgreSQL (CI/CD Ready)

## 🎯 Ключевые достижения и Техническая Архитектура

Этот проект демонстрирует полный цикл разработки современного бэкенд-приложения, с акцентом на контейнеризацию и автоматизацию тестирования и сборки.

### 1. Стек и Асинхронная Разработка
* **Backend**: Разработан на **FastAPI**, используя принципы асинхронного программирования (Async/Await) для высокопроизводительного API.
* **База Данных**: Использован **PostgreSQL** в контейнере, управляемый ORM **SQLAlchemy** в асинхронном режиме.

### 2. Мульти-сервисная Контейнеризация (Docker)
Проект оркестрован через **Docker Compose** и состоит из четырех независимых, но взаимосвязанных сервисов:
* `db`: Контейнер PostgreSQL с проверкой готовности (`healthcheck`).
* `backend`: Контейнер FastAPI с кодом приложения.
* `frontend`: Отдельный контейнер для обслуживания статического фронтенда.
* `tests`: **Изолированный контейнер** для выполнения Pytest, который подключается к `db`.

### 3. Полный Конвейер CI/CD (GitHub Actions)
Настроен автоматизированный конвейер в `ci.yml` с использованием **GitHub Actions**, который гарантирует качество и готовность к развертыванию:
* **Job 'test'**: Автоматически запускает **Pytest** в Docker-контейнере при каждом пуше в `main`. **Тесты проходят только при успешном подключении к БД**.
* **Job 'build_push'**: **(CD)** После успешного тестирования, Job выполняет вход в Docker Hub, собирает **два отдельных Docker-образа** (`backend` и `frontend`) и публикует их в реестр.

### 4. Готовность к Развертыванию
Конвейер полностью готов к финальной стадии CD. Для развертывания на удаленный сервер требуется лишь добавление SSH-кредитенций и запуск `docker compose pull / up`.

---

## ⚙️ Как запустить локально
Для запуска проекта достаточно наличия Docker.
1.  Клонируйте репозиторий.
2.  Создайте файлы `.env` и `.env-db` (с указанием секретов).
3.  Запустите все сервисы в фоновом режиме:
    ```bash
    docker compose up --build -d
    ```
